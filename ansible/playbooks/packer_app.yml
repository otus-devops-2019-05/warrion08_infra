---
- name: Install Ruby and Bundler
  hosts: all
  become: true

  roles:
    -app

#  tasks:
#  - name: Install ruby and packages
#    apt: "name={{ item }} state=present"
#    with_items:
#      - ruby-full
#      - ruby-bundler
#      - build-essential