---
- name: Install Ruby and Bundler
  hosts: app
  become: true
  tasks:
  - name: Install ruby and packages
    apt: "name={{ item }} state=present"
    with_items:
      - ruby-full
      - ruby-bundler
      - build-essential
      